// Function Expression (hàm biểu thức): nghĩa là gán hàm cho một biến
// cũng là 1 cách định nghĩa hàm.

const greet = function (name) {
    console.log(`Hello, ${name}!`);
};

greet('John'); // Output: Hello, John!